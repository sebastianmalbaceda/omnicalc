import { PricingSection } from '@/components/pricing/PricingSection';

export const metadata = {
  title: 'Pricing — OmniCalc',
  description:
    'Choose the plan that works for you. Free for basic calculations, Pro for scientific functions and cloud sync.',
};

export default function PricingPage() {
  return <PricingSection />;
}
